# Codex 集成需求文档

> 版本：v1.6（定稿）
> 日期：2026-08-03
> 关联：Plane CE 1.4.0（`preview` 分支 fork），部署与交接见 [部署说明.md](./部署说明.md)、[交接文档.md](./交接文档.md)

> 决策记录：v1.1 与开发对齐后定稿——① 卡点/退回**直接改回上一环节状态**，不设独立"退回中"状态；② 状态推进**完全原生自由**，不加自定义权限校验；③ v1.5 起**自动化进入实施**；④ v1.2 起 **Codex 按流程角色一账号一机器人（方案 B）**：评估/拆分/执行/验收各一个账号，均 MEMBER(15)；⑤ v1.3 `codex_bot` 已清理；⑥ v1.4 确认父子任务流转规则与执行者自推进；⑦ v1.5 自动化落地（WebHook + 扫描，三环节自动，验收人工）；⑧ **v1.6（2026-08-04）会话绑定与全自动验收**：每单子绑定三个会话（评估会话=评估+拆分共用、执行会话、验收会话），有历史则 resume（resume 需显式 thread_id，沙箱用 `-c sandbox_mode` 覆盖）；**验收改为 `codex_verify` 自动黑盒验收**（单子需求 + git worktree 隔离）；子任务**直接进入待执行**（跳过评估/拆分）、共用父任务拆分会话、**最多拆一次不递归**；父任务部分成功部分失败收尾（全部完成→已完成；部分完成+部分取消→已完成并注明；未终态→等待；拆分方案被推翻→人工改回评估中）；返工 resume 执行会话，**验收连续 REJECT 达 3 轮 → 评论需人工介入并回"待评估"**。

---

## 1. 目标

把 Plane 当作**任务流程工具本身**来使用；流程中的"执行者"角色由 **Codex**（机器人成员）承担，其余角色与交互保持原生。

一句话：**不是给 Plane 加流程，而是把流程里的人换成 Codex。**

---

## 2. 核心决策（已确认）

1. **不做自定义工作流覆盖层**：不新增流程字段、API、前端面板。此前实现已全部移除（提交 `fd35372`，迁移 `0124` 删除字段）。
2. **Codex = 普通项目成员**：`MEMBER(15)`，非管理员；可被指派为任意环节的负责人（主要是执行者）。
3. **环节 = Plane 自定义状态**；**当前环节负责人 = assignee**；**任务单 = 子任务**（原生父子 issue）。
4. **卡点/退回 = 评论 + @上一级 + 状态直接改回上一环节状态**（无独立"退回中"状态）；**验收 = 验收人把状态推进到"已完成"**。
5. **Codex 配置不进数据库、不进 git**：API token 与代码路径存于 `apps/api/.env`（gitignored）。
6. **本期不做自动化**：不写 runner/脚本轮询；Codex 执行由人触发，回写由 Codex 通过 API 手动完成。
7. **状态推进不设自定义权限校验**：任何项目成员均可推进状态（原生行为），环节纪律靠流程约定与评论留痕约束。
8. **Codex 按流程角色拆分为多个账号**：评估者 `codex_eval`、拆分者 `codex_split`、执行者 `codex_exec`、验收者 `codex_verify`，各自独立 API token；"角色"是账号维度而非 Plane 权限角色，所有账号均为 `MEMBER(15)`。

---

## 3. Codex 账号

按流程角色一账号一机器人（方案 B），账号均为 `is_bot=True / bot_type=CODEX`（禁止交互式登录，只能走 API token），工作区 `workingcatpet` 与项目 `4c9803a4` 角色均为 **MEMBER(15)**：

| 流程角色 | 账号 | token（`apps/api/.env`，gitignored） | user id（`.env`） |
|---|---|---|---|
| 评估者 | `codex_eval` | `CODEX_EVAL_TOKEN` | `CODEX_EVAL_USER_ID` |
| 拆分者 | `codex_split` | `CODEX_SPLIT_TOKEN` | `CODEX_SPLIT_USER_ID` |
| 执行者 | `codex_exec` | `CODEX_EXEC_TOKEN` | `CODEX_EXEC_USER_ID` |
| 验收者 | `codex_verify` | `CODEX_VERIFY_TOKEN` | `CODEX_VERIFY_USER_ID` |

- 早期 `codex_bot` 已于 2026-08-03 清理（token 已吊销、账号已删除），不再存在；
- `CODEX_FRONTEND_PATH` 存于 `.env`（gitignored），代码路径所有角色共用；后端路径暂无。

---

## 4. 原生流程用法

### 4.1 状态（在项目设置中创建自定义状态）

```mermaid
flowchart LR
    A["待评估"] --> B["评估中"]
    B --> C["待拆分"]
    C --> D["拆分中"]
    D --> E["待执行"]
    E --> F["执行中"]
    F --> G["待验收"]
    G --> H["验收中"]
    H --> I["已完成"]
    H -->|"功能缺陷"| F
    H -->|"拆分/依赖问题"| C
    F -.->|"卡点：评论 + @上一级 + 状态改回"| C
    C -.->|"卡点"| B
    B -.->|"卡点"| A
```

（无独立"退回中"状态；虚线为逐级上抛路径：卡点/退回时评论说明 + @上一级，状态**直接改回上一环节状态**。）

状态分组（Plane 原生 group，决定看板泳道与完成语义）：

| 分组 | 状态 |
|---|---|
| backlog | 待评估 |
| unstarted | 评估中、待拆分 |
| started | 拆分中、待执行、执行中、待验收、验收中 |
| completed | 已完成 |
| cancelled | 取消 |

### 4.1.1 父子任务流转规则（v1.4 确认）

- 拆分者（`codex_split` 或人）在"拆分中"判断任务**是否需要拆分**；
- **不需要拆分**：父任务直接作为流转单元继续流程（拆分中→待执行→…→已完成），不再创建子任务；
- **需要拆分**：父任务**留在拆分者处**（停在"拆分中"），拆分者创建子任务；子任务各自独立走完整流程；
- 全部子任务完成（均已到"已完成"）后，父任务再流转：全部验收通过 → 父任务推进到"**已完成**"；失败/无法达成 → 父任务状态回到"**评估中**"、assignee 改回评估者，重新评估。

### 4.2 日常操作

| 动作 | 原生方式 |
|---|---|
| 发起问题 | 创建 issue |
| 拆分任务 | 拆分者判断是否拆分：不需拆分则父任务直接走流程；需拆分则创建子任务（父 issue 下），父任务停在拆分者处 |
| 指派环节负责人 | 设置 assignee（评估者/拆分者/执行者/验收者按阶段换人） |
| 推进环节 | 当前负责人改状态；**待执行→执行中由执行者自行推进**（Codex 开始执行时推） |
| 卡点/退回 | 评论说明 + `@上一级` + 状态直接改回上一环节状态（如 拆分中→评估中） |
| 验收通过 | 验收人推进到"已完成" |
| 验收返工 | 退回执行者（状态改回"执行中"） |
| 完成问题 | 不需拆分：父任务走完流程，验收人推进到"已完成"；需拆分：全部子任务完成后父任务流转（成功→已完成，失败→返回评估者） |

### 4.3 Codex 执行

1. 按当前环节把对应角色账号指派为负责人：评估中→`codex_eval`、拆分中→`codex_split`、执行中→`codex_exec`、验收中→`codex_verify`（assignee = 对应账号）；
2. 该角色 Codex 用**自己的 token** 通过 API/对话读取任务内容（含 `CODEX_FRONTEND_PATH` 定位代码）；
3. `codex_exec` 被指派且任务状态=待执行时，**自行把状态推进到"执行中"**开始干活，完成后推进到"待验收"并评论说明改动；
4. `codex_verify` 验收：通过则推进到"已完成"；不通过则退回执行者（状态改回"执行中"）或按 4.1.1 返回。

---

## 5. 权限边界

- Codex 只拥有 **MEMBER(15)** 权限：可创建/编辑 issue、评论、推进状态（项目内）；
- 管理员（ADMIN=20）由人担任；Codex 不授予管理员；
- 成员列表已放行 `bot_type=CODEX`，Codex 可被正常指派；
- 状态推进为原生自由行为，任何人（MEMBER 及以上）均可改状态；环节纪律靠流程约定维护，不做自定义校验。
- 四个角色账号权限完全相同（都是 MEMBER），角色差异体现在"指派给谁 + 当前状态"；如需角色级权限差异（如只有验收账号能推进到"已完成"），需引入自定义逻辑，超出当前范围。

---

## 6. 自动化（v1.5 确认，本期实施）

> v1.6 更新：验收环节由人工改为 `codex_verify` 自动黑盒验收；引入会话绑定。

### 6.1 目标与范围

- **全自动闭环**：runner 自动发现任务 → 自动触发对应角色 Codex → 自动回写状态/评论；
- 自动化环节：**评估（`codex_eval`）、拆分（`codex_split`）、执行（`codex_exec`）、验收（`codex_verify`）**；
- **验收 = `codex_verify` 自动黑盒验收**：基于单子需求/验收标准 + git（分支/diff，在 worktree 隔离副本中），能跑则运行验证、跑不了做静态核对；PASS→已完成，REJECT→问题清单评论+状态回"待执行"。

### 6.1.1 会话绑定（v1.6）

- 每个单子最多绑定三个 Codex 会话：**评估会话**（评估+拆分共用）、**执行会话**、**验收会话**；子任务的"拆分相关会话"= **父任务的拆分会话**（thread_id 共享），子任务自身只有执行会话、验收会话；
- 状态流转到对应环节时：**该环节会话有历史（thread_id 存在）→ resume；没有 → 新开**；返工 resume 执行会话，二次验收 resume 验收会话；
- 技术要点（已验证）：thread_id 从 `codex exec --json` 首事件捕获；resume 跟随进程 cwd（runner 用 spawn cwd 控制）；resume 沙箱用 `-c sandbox_mode="read-only|workspace-write"` 覆盖；**必须显式 thread_id，禁用 --last**（会误捡桌面会话）；
- 降级兜底：resume 失败 → 新开会话 + 重注入单子上下文，不阻塞流程。

### 6.1.2 拆分与深度（v1.6）

- **子任务直接进入"待执行"**（跳过评估/拆分）：拆分者产出子任务即视为可执行单元，子任务描述自带 仓库/需求/验收标准；
- **最多拆一次，不允许递归拆分**：深度≥1 的任务不再拆分；复杂子任务走人工例外（改回"待拆分"，用父任务拆分会话重新判断，但不得再产生孙任务）；
- 评估只发生在源头（人工创建的任务）。

### 6.1.3 父任务收尾（部分成功部分失败，v1.6）

- 父任务停在"拆分中"，等待**全部子任务进入终态**（已完成 ∪ 取消）；
- 全部已完成 → 父任务 → 已完成；
- 部分完成 + 部分取消 → 父任务 → 已完成（**部分交付**），评论注明哪些已交付、哪些被取消及原因（取消是人工决策，评论留痕）；
- 存在未终态子任务（含返工循环中）→ 父任务继续等待，不提前判定；
- 拆分方案被推翻/需求变更 → **人工**把父任务改回"评估中"（resume 父评估会话），自动化不因单个子任务失败回退父任务。

### 6.2 触发机制（WebHook 为主 + 扫描兜底）

- Plane 工作区配置 **webhook**（事件：issue / issue_comment），推送到 runner 本地监听服务；webhook 带签名与投递日志（Plane 原生支持）；
- **前提改动**：Plane 当前禁止 localhost 回环 webhook 地址，需放开（`WEBHOOK_ALLOWED_IPS=127.0.0.1` + 校验放宽），属自动化基础设施改动；
- **兜底**：runner 启动时 + 空闲 5 分钟做一次全量扫描（按角色+状态查询），防 webhook 丢失/服务重启漏事件；
- **防环**：bot 自身的状态推进也会触发 webhook，runner 按"当前状态+assignee+in-flight"幂等判断，忽略自身引起的无效事件。

### 6.3 环节自动化规则

| 环节 | 触发条件 | runner 动作 |
|---|---|---|
| 评估 | assignee=`codex_eval` 且 state=待评估 | 触发评估 agent（`codex exec`，只读分析）→ 评论评估结论 → 通过则推进到"待拆分"并把 assignee 交给 `codex_split`；不通过/信息不足 → 评论 + 状态回退"待评估" |
| 拆分 | assignee=`codex_split` 且 state=待拆分 | 触发拆分 agent → 判断：不需拆分 → 推进"待执行"并把 assignee 交给 `codex_exec`；需要拆分 → 创建子任务（父任务留在"拆分中"），子任务各自走流程 |
| 执行 | assignee=`codex_exec` 且 state=待执行 | 触发执行 agent（在**单子 worktree** 中，分支 `codex/<完整单号>`）→ 推进"执行中"→ **只改文件** + 自检 + 评论（含黑盒验证步骤）→ **runner 侧 commit** 本地分支（不 push）→ 推进"待验收"、assignee 交给 `codex_verify`；返工（有历史）→ resume 执行会话 |
| 验收 | assignee=`codex_verify` 且 state=待验收 | 触发验收 agent（**同一 worktree**，workspace-write 但禁改源码）→ 对照单子需求/验收标准 + git diff **黑盒验收** → PASS → 推进"已完成"+清理 worktree；REJECT → 问题清单评论 + 状态回"待执行"；二次验收 → resume 验收会话 |

- **子任务**：拆分生成时直接进入"待执行"（跳过评估/拆分），深度=1，共用父任务拆分会话；不再递归拆分（见 6.1.2）。
- **父任务收尾**：见 6.1.3（全部完成→已完成；部分完成+部分取消→已完成并注明；未终态→等待；拆分方案被推翻→人工改回评估中）。
- **返工统一**：所有回退/返工统一回到"**待X**"状态（待评估/待拆分/待执行），触发条件唯一化，避免 runner 对"X中"状态误判重复执行。
- **返工上限**：验收连续 REJECT 达 **3 轮** → 评论"需人工介入" + 状态回"待评估"（assignee 交回 `codex_eval`）。

### 6.4 执行与提交（护栏）

- `codex exec` 在**任务指定的子仓库的单子 worktree** 中运行（每个单子一个固定路径的 worktree，路径 `<仓库父目录>\.codex-wt\<单子id>`），主工作区不被触碰；
- 执行 agent **只改文件、不执行 git 命令**；跑完后由 **runner 侧 commit** 到本地分支（分支名 `codex/<完整单号>`，如 `codex/2026080201-26`，单号=项目标识-序号），**不 push**；验收与返工在同一 worktree 进行，单子完成后清理 worktree；
- 自检按子仓库约定：TS 项目 `tsc/lint`、Gradle `gradle build`、Unity 按项目配置；自检失败视为执行失败；
- 超时保护（单任务最长执行时长，超时按失败处理）；执行日志落盘 `tools/codex-runner/logs/`。

### 6.5 失败处理

- 任一环节失败：runner **评论说明原因 + 状态回退到该环节"待X"**，保留 assignee=对应角色；
- **不自动重试**（agent 失败）：避免死循环；人工查看评论后决定重派/修复/改状态；
- **验收返工**属于设计内的循环：REJECT → resume 执行会话修复 → 二次验收；连续 3 轮 REJECT → 转人工。

### 6.6 并发与幂等

- 每个角色同一时刻**只执行一个任务**（串行队列）；
- in-flight 记录（本地状态文件）防止同一任务重复触发；
- 状态本身即锁：推进到"X中"后不再匹配"待X"触发条件。

### 6.7 任务描述规范

- 任务 description 必须写明：**目标子仓库**、需求说明、**验收标准**（结构化提取 仓库/需求/验收标准）；
- runner 以"单子绑定上下文（结构化基础信息 + 前面环节结论 + 父单子信息 + 评论历史）+ 会话记忆 + 环节指令"拼 prompt；
- 无法定位子仓库的任务 → runner 评论提示补充信息，状态回退"待X"，不执行。

### 6.8 交付物

- runner 工具：`tools/codex-runner/`（独立 Node 脚本，不并入 turbo workspace）；
- Plane 侧小改动：放开 localhost webhook 校验；
- 配置：工作区 webhook（issue/issue_comment 事件）+ `apps/api/.env` 增加 `WEBHOOK_ALLOWED_IPS`；
- v1.6 新增：会话绑定（thread_id 捕获/resume + `-c sandbox_mode` 覆盖）、验收自动化（worktree + 黑盒）、父任务部分成功收尾、返工上限。

---

## 7. 已保留的工程改动（非工作流）

- 前端翻译中文化（活动记录、状态名、相对时间等）；
- 成员列表放行 CODEX 机器人、序列化器暴露 `bot_type`；
- nginx `/locales/` 与 `index.html` no-cache、API 请求 `Cache-Control: no-cache`；
- `entry.client.tsx` 用 `createRoot`（消除 hydration 报错）；
- 移除付费相关 UI。

---

## 8. 历史说明

- v0.1–v0.3 曾设计一套自定义工作流（流程字段、退回/上抛 API、前端面板），经评估后**方向推翻**：Plane 本身即是流程工具，自定义覆盖层反而割裂使用体验；
- 相关代码已全部移除（`fd35372`），如需追溯见 git 历史与 [交接文档.md](./交接文档.md)。
- v1.1（2026-08-03）：与开发对齐后定稿，统一卡点语义（直接改回上一环节状态）、明确权限边界与本期范围（不做自动化）；项目状态已按第 4.1 节建好。
- v1.2（2026-08-03）：按流程角色拆分 Codex 账号（方案 B），一角色一账号一 token；`codex_bot` 降级为兼容账号。
- v1.3（2026-08-03）：`codex_bot` 已清理（含多余 token 吊销），仅保留 `codex_eval/split/exec/verify` 四个角色账号。
- v1.4（2026-08-03）：中途对需求确认父子任务流转规则（见 4.1.1）与"待执行→执行中由执行者自行推进"；已用"测试任务"走通完整流程。
- v1.5（2026-08-03）：自动化需求定稿（见第 6 节），推翻 v1.1 的"不做自动化"；全自动闭环 + WebHook 触发，评估/拆分/执行自动、验收人工。
- v1.6（2026-08-04）：会话绑定（一单三会话、有历史则 resume、显式 thread_id）、验收改为 `codex_verify` 自动黑盒验收（worktree 隔离）、子任务直接进入待执行且最多拆一次、父任务部分成功部分失败收尾、返工 3 轮上限。
- v1.6.1（2026-08-04）：实现修正——执行提交改由 **runner 侧执行**（agent 只改文件，解决 worktree 沙箱无法写 `.git` 的问题）；分支/提交信息使用**完整单号**（如 `codex/2026080201-26`）；任务描述格式放宽（仓库缺省用默认仓库，需求/验收标准自然语言即可）。

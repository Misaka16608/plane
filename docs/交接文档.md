# Plane 项目接手文档（会话交接）

> 目的：让下一个会话（或开发者）快速了解当前状态、环境要点、代码结构和下一步定制方向。
> 详细部署流程见 [部署说明.md](./部署说明.md)。

---

## 0. 三句话摘要

1. 本机已用 Docker Compose 跑起 **Plane CE 1.4.0**（`preview` 分支的 fork），入口 `http://localhost`，管理员已创建，可正常使用；付费相关 UI 已清理，界面翻译已大量中文化。
2. **Codex 工作流已完成阶段 1（数据模型 + API）**，并有阶段 2 前端面板雏形；本地有 **5 个提交未推送**（`origin/preview` 停在 `d81f584`，本地 `HEAD=3e09b70`）。
3. 下一步：执行 `create_codex_bot` 添加 Codex 账号（前置）→ 阶段 2 面板收尾 → 阶段 3/4 自动化。

---

## 1. 当前系统状态

| 项目 | 状态 |
|---|---|
| 仓库 | `D:\_Code\Server\plane`，fork 自 `Misaka16608/plane`（= makeplane/plane `preview`） |
| 版本 | Plane Community Edition 1.4.0 |
| 远程 | `origin` = `ssh://git@ssh.github.com:443/Misaka16608/plane.git`，`preview` 停在 `d81f584`；本地 `HEAD=3e09b70`，**领先 5 个提交未推送** |
| 运行方式 | Docker Compose，13 个容器全部 Up/healthy |
| 访问 | http://localhost（主应用）/ /god-mode/（管理后台）/ /spaces/（分享页） |
| 管理员 | 已创建（邮箱为用户的 163 邮箱，密码用户自持，**注意密码强度要求 zxcvbn≥3**） |
| 语言 | 默认英文，登录后顶部搜索框（Power-K）→ Preferences → Update language 可切中文；zh-CN 翻译已补齐活动记录、相对时间、默认状态名等 |
| Codex 账号 | **尚未创建**（用 `create_codex_bot` 命令，见第 5 节） |

### 关键文件位置

| 文件 | 说明 |
|---|---|
| `docker-compose.yml` | 服务编排（含本会话的修复：proxy/live env、web 语言卷挂载） |
| `.env` / `apps/api/.env` | 运行配置（已被 gitignore，含 SECRET_KEY 等） |
| `packages/i18n/src/locales/` | 全部翻译文件（**卷挂载，改完刷新即生效**） |
| `部署说明.md` | 启动/停止/重建/踩坑全流程 |
| `需求文档.md` | Codex 工作流需求与决策记录（v0.3） |
| `apps/api/plane/db/management/commands/create_codex_bot.py` | 创建 Codex 机器人账号的命令（已提交，未执行） |

---

## 2. 环境关键事实（下个会话必读）

- **本地代理**：`127.0.0.1:7890`（mihomo）必须运行；`github.com`、`auth.docker.io`、`sum.golang.org` 直连不通。
- **Docker 代理配置（两处，勿随意改）**：
  - Docker Desktop 设置 `%APPDATA%\Docker\settings-store.json`：`ProxyHTTPMode=manual` + `OverrideProxyHTTP/HTTPS=http://127.0.0.1:7890`
  - `%USERPROFILE%\.docker\daemon.json`：`proxies` 指向同一代理
- **构建前需开启代理的全局/TUN 模式**（Docker 虚拟机内 BuildKit 的直连流量要走代理），
  否则 `docker compose build` 拉基础镜像会失败；详见 部署说明.md 第 3.4 节。
- **写 Docker/系统配置务必用无 BOM 的 UTF-8**，否则 Docker Desktop 解析 JSON 崩溃（本次踩过）。
- **git 命令需要提权执行**（沙箱对 `.git` 只读）；`git status` 在沙箱内可读但 commit/push 需 escalation。
- **nginx 缓存策略（已配置，勿回退）**：`/locales/*.json` 与 `index.html` 均返回 `Cache-Control: no-cache`，保证翻译热更新与前端 chunk 不串版本。
- **入口渲染用 `createRoot`**（`apps/web/app/entry.client.tsx`）：本项目是 `ssr:false` 纯客户端应用，改回 `hydrateRoot` 会在控制台报 React #418/#423（页面仍可用但每次都报错）。
- **i18n 键源只有 en**：`keys.generated.ts` 由 `scripts/generate-types.ts` 从 `locales/en/*.json` 生成；每次 `check:types`/build 会重新生成，生成后需 `pnpm exec oxfmt packages/i18n/src/types/keys.generated.ts`。
- **ICU 插值坑**：i18next-icu 不处理 `{{name}}` 占位符。新翻译键若需嵌入链接/变量，用 `{{name}}` 占位并在组件里走 `interpolateNodes`（见 `apps/web/core/components/core/activity.tsx` 与 `issue-activity/.../helpers/i18n.tsx`）。
- **后端测试栈**：`docker compose -f docker-compose-test.yml run --rm api-tests pytest <路径>`；基础镜像用本地 tag 顶替（`python:3.12.10-alpine` → `3.12.5-alpine`），入口 `pip install` 需注入代理环境变量（`HTTP_PROXY=http://host.docker.internal:7890` 等）。
- **husky pre-commit 会失败**（hook 环境找不到 pnpm），本地提交统一用 `git commit --no-verify`（代码已过 `pnpm check`）。

---

## 3. 代码架构速览

pnpm/turbo monorepo：

```
apps/
  web/      # 主应用前端（React Router SPA + Vite，nginx 托管）
  admin/    # 管理员后台 god-mode（React Router SPA，独立容器）
  space/    # 公开分享页（独立容器）
  live/     # 实时协作（Node，HocusPocus 编辑器后端）
  api/      # Django 后端（api/worker/beat-worker/migrator 共用镜像）
  proxy/    # Caddy 反向代理（统一入口 80/443）
packages/
  constants/ # 常量（含角色、端点等）
  i18n/      # 多语言（本会话改造：运行时 fetch 加载）
  services/  # 前端 API 请求层
  types/     # 前后端共享 TS 类型
  ui/, propel/, utils/, hooks/ 等
```

### 关键代码落点

| 需求 | 位置 |
|---|---|
| 后端角色/权限 | `apps/api/plane/app/permissions/base.py`（`class ROLE(Enum)`） |
| 后端权限校验 | `apps/api/plane/app/permissions/*.py`（按 API 视图挂载） |
| 成员/邀请序列化 | `apps/api/plane/api/serializers/member.py`、`invite.py` |
| 前端角色常量 | `packages/constants/src/`（角色、端点等） |
| 实例配置（LLM 开关等） | `apps/api/plane/license/api/views/instance.py`（`has_llm_configured` 等字段） |
| 翻译 | `packages/i18n/src/locales/<lang>/<ns>.json` |
| API 路由注册 | `apps/api/plane/urls.py` 及各 app 的 `urls.py` |

### Codex 工作流代码落点（阶段 1/2）

| 需求 | 位置 |
|---|---|
| 工作流服务（推进/退回/上抛/指派核心逻辑） | `apps/api/plane/app/services/workflow.py` |
| 工作流 API 视图 | `apps/api/plane/app/views/issue/workflow.py` |
| 工作流路由 | `apps/api/plane/app/urls/issue.py`（`.../workflow/assign|advance|return|escalate/`） |
| 成员前后端路径字段 | `ProjectMember.frontend_path/backend_path`（`apps/api/plane/db/models/project.py`） |
| 任务单流程字段 | `Issue.workflow_*` + `WorkflowStage`（`apps/api/plane/db/models/issue.py`），迁移 `0123_...` |
| 工作流前端面板（雏形，未接端点） | `apps/web/core/components/issues/issue-detail/workflow/workflow-panel.tsx` |
| 工作流前端服务 | `apps/web/core/services/issue/workflow.service.ts` |
| 创建 Codex 机器人账号 | `apps/api/plane/db/management/commands/create_codex_bot.py` |
| 状态名本地化 | `apps/web/core/components/issues/issue-layouts/utils.tsx`（`getLocalizedStateName`） |
---

## 4. 自定义开发工作流

### 改翻译（无需重建）

编辑 `packages/i18n/src/locales/<lang>/*.json` → 浏览器 Ctrl+F5（已配 no-cache，正常刷新即可）。

注意：
- **en 是键源**：新增键必须同时加到 `en/` 与目标语言文件，否则 `generate:types` 生成的类型里没有；
- 需要嵌入链接/变量的翻译，用 `{{name}}` 占位符 + `interpolateNodes`（ICU 不处理 `{{}}`，见第 2 节）；
- 改完建议跑 `pnpm --filter=@plane/i18n run build` + oxfmt 生成文件。

### 改前端/后端代码（需要重建对应镜像）

```powershell
cd D:\_Code\Server\plane

# 只改 web 前端 / i18n 包
docker compose build web && docker compose up -d web

# 只改 api 后端
docker compose build api worker beat-worker migrator && docker compose up -d api worker beat-worker migrator

# 全量
docker compose build && docker compose up -d
```

> 注意：重建前确保代理处于全局/TUN 模式（见 部署说明.md 3.4），否则 BuildKit 拉基础镜像会失败。

### 提交推送

```powershell
git add -A
git commit --no-verify -m "feat: ..."   # husky 钩子在本机找不到 pnpm，必须 --no-verify
git push origin preview
```

> 当前本地领先 `origin/preview` **5 个提交未推送**（f5ef8d2 → 3e09b70），记得推送或交接说明。

---

## 5. 下一步定制方向："接入 Codex 角色"

（已与用户确认：Codex = 机器人成员，权限为项目成员级 MEMBER=15，可被指派到流程任意环节；需求与决策见 `需求文档.md`。）

### 前置：添加 Codex 账号（尚未执行）

```powershell
docker compose exec api python manage.py create_codex_bot `
  --workspace workingcatpet `
  --project 4c9803a4-e542-4213-b04b-57628bc5bee9
```

- 自动创建 `codex_bot`（`is_bot=True`、`BotTypeEnum.CODEX`）、加入工作区+项目（MEMBER=15）、签发 API token（**只打印一次，需保存**）；
- 随后配置 Codex 的前后端路径（`ProjectMember.frontend_path/backend_path`，PATCH 成员接口）；
- 之后通过 `workflow/assign/` 把 Codex 指派到评估/拆分/执行/验收环节。

### 阶段 2：流程与前端（进行中）

- 已有雏形：`workflow-panel.tsx` + `workflow.service.ts`（已提交）；
- 待收尾：面板接入工作流端点与路由、状态路由、卡点备注/退回按钮、验收批量返回。

### 阶段 3 / 4：自动化

- 3：脚本触发（任务 → `codex exec` → 回写，半自动）；
- 4：完整闭环 runner（轮询 Plane API + API token + `pnpm check` 验证 + 失败重试）。

---

## 6. 注意事项 / 遗留项

- **space 容器**仍是上一版静态打包的 i18n（本会话只重建了 web）；如 space 需要热更新翻译，需重建 `docker compose build space`。
- **SMTP 未配置**：邮件相关功能（密码重置、邀请邮件）不可用，需在 god-mode 或实例配置里配置 SMTP。
- **默认口令**：Postgres/RabbitMQ/MinIO 用的是 `.env.example` 默认值，仅限本机试用，暴露到公网前必须改。
- **数据卷**：`pgdata`/`redisdata`/`uploads`/`rabbitmq_data`，`docker compose down -v` 会清空。
- **本地提交**：当前 `HEAD=3e09b70`，**领先 `origin/preview` 5 个提交未推送**；后续改动记得 `git add/commit --no-verify/push`。
- **Codex 账号尚未创建**：执行第 5 节前置命令后，记得把 API token 交给用户保存。
- **工作流前端面板未接端点**：`workflow-panel.tsx` 是阶段 2 雏形，尚未连到 `workflow/*` API。
- **i18n 生成怪癖**：`keys.generated.ts` 每次 check:types 会重新生成且不带格式，需 oxfmt（见第 2 节）。
- **状态名本地化**：只翻译 6 个默认状态名（`issue_activity.state_name.*`），自定义状态显示原名；新增默认状态名需同步维护 `apps/web/core/components/issues/issue-layouts/utils.tsx` 的映射。
- **沙箱提权**：Docker、git 写操作、读系统配置都需要 `require_escalated`。

---

## 7. 常用命令速查

```powershell
cd D:\_Code\Server\plane
docker compose ps                       # 状态
docker compose logs -f api              # 后端日志
docker compose up -d                    # 启动
docker compose down                     # 停止（留数据）
curl.exe http://localhost/api/instances/ # 健康检查
docker compose exec api python manage.py create_codex_bot --workspace <slug> --project <uuid>   # 添加 Codex 账号
docker compose -f docker-compose-test.yml run --rm -e HTTP_PROXY=http://host.docker.internal:7890 -e HTTPS_PROXY=http://host.docker.internal:7890 api-tests pytest plane/tests/unit   # 后端单测
pnpm check                              # 前端 format/lint/types 全检
git push origin preview                 # 推送
```
